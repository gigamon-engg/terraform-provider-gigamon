---
layout: 'gigamon'
page_title: 'Gigamon: gigamon_app_pcapng'
subcategory: 'Applications'
description: |-
  Manages a PCapNG packet-capture application instance on a GigaVUE-FM monitoring session.
---

# gigamon_app_pcapng (Resource)

**PCapNG** captures raw packets from a monitoring session and writes them to rotating `.pcapng` files. Use `packet_filter` to scope the capture with a BPF expression, source/destination IPs, and VLAN IDs. Configure `output_config` for file path, rotation, and compression. Tune `performance` to size the ring buffer and set the per-packet snapshot length.

## Example Usage

```terraform
# Minimal typed PCapNG example.
# This example uses an existing monitoring session that is imported.

terraform {
  required_providers {
    gigamon = {
      source = "local/gigamon/gigamon"
    }
  }
}

provider "gigamon" {
  fm_address  = "10.114.50.20"
  skip_verify = true
  api_token   = "eyJhbGciOiJIUzI1NiJ9.eyJ0b2tlbklkIjoiNDYxMDgyNDM1NDEzOTY5NCIsInN1YiI6Imdtb2hhbiIsImlhdCI6MTc4MTUxMjMyMywiZXhwIjoxNzg0MTA0MzIzfQ.mlP_dTGCIB42Y3PjpwoH6iKdlxFjDPktDBmdl1WFDhU"
}

# Store your existing monitoring session ID locally
# Format: monitoringSession::<platform>::<uuid>
locals {
  monitoring_session_id = "monitoringSession::vmware::0ddfdd2d-2a27-4abc-ae39-3432601bcd53"
}

resource "gigamon_app_pcapng" "minimal" {
  monitoring_session_id = local.monitoring_session_id
  capture_mode          = "continuous"

  packet_filter = {
    bpf_syntax  = "tcp and port 443"
    source_ip   = "192.0.2.10"
    dest_ip     = "198.51.100.10"
    vlan_filter = [100, 200]
  }

  output_config = {
    file_path     = "/var/log/gigamon/pcapng/capture.pcapng"
    max_file_size = 500
    rotation      = true
    compression   = "gzip"
  }

  performance = {
    buffer_size    = 64
    thread_count   = 4
    packet_snaplen = 65535
  }
}
```

## Argument Reference

The following arguments are supported:

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `capture_mode` (String) Capture mode: continuous, on-demand, or triggered
- `monitoring_session_id` (String) The ID of the monitoring session

### Optional

- `output_config` (Attributes) Output configuration (see [below for nested schema](#nestedatt--output_config))
- `packet_filter` (Attributes) Packet filter configuration (see [below for nested schema](#nestedatt--packet_filter))
- `performance` (Attributes) Performance configuration (see [below for nested schema](#nestedatt--performance))

### Read-Only

- `id` (String) The unique identifier for the PCapNG app resource

<a id="nestedatt--output_config"></a>
### Nested Schema for `output_config`

Optional:

- `compression` (String) Compression: none, gzip, or xz
- `file_path` (String) Output file path
- `max_file_size` (Number) Max file size in MB: 1-10000
- `rotation` (Boolean) Enable file rotation


<a id="nestedatt--packet_filter"></a>
### Nested Schema for `packet_filter`

Optional:

- `bpf_syntax` (String) BPF filter syntax
- `dest_ip` (String) Destination IP filter
- `source_ip` (String) Source IP filter
- `vlan_filter` (List of Number) VLAN IDs to filter


<a id="nestedatt--performance"></a>
### Nested Schema for `performance`

Optional:

- `buffer_size` (Number) Buffer size in MB: 4-1024
- `packet_snaplen` (Number) Packet snaplen: 64-65535
- `thread_count` (Number) Thread count: 1-16




## Attribute Reference

In addition to all arguments above, the following attributes are exported:

- `id` - (String) Typed resource identifier of the form `app::<type>::<uuid>`. Used for cross-resource references and import.

## Import

PCapNG app resources can be imported using the monitoring session ID and the raw resource UUID:

```shell
terraform import gigamon_app_pcapng.example "<monitoring_session_id>::<raw_uuid>"
```
